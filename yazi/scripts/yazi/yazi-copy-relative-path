#!/usr/bin/env bash
set -euo pipefail

file="${1:?file argument required}"
dir="$(dirname "$file")"

if root="$(git -C "$dir" rev-parse --show-toplevel 2>/dev/null)"; then
    relative="${file#$root/}"
else
    relative="$file"
fi

printf '%s' "$relative" | pbcopy
